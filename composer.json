{
    "name": "nahidhk/invoice-app",
    "description": "This app is useful as a dipcol invoice app",
    "require": {
        "php": ">=7.4"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "start": "php -S localhost:8000 -t public"
    }
}